<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
		xmlns:tools="http://schemas.android.com/tools"
		android:orientation="vertical"
		android:layout_width="match_parent"
		android:layout_height="match_parent"
		android:paddingStart="@dimen/activity_horizontal_margin"
		android:paddingEnd="@dimen/activity_horizontal_margin"
		android:paddingTop="@dimen/activity_vertical_margin"
		android:paddingBottom="@dimen/activity_vertical_margin">

	<!-- title and icon -->
	<TextView
			android:id="@+id/dialog_title"
			style="@style/TextStyle.Headline"
			android:layout_width="match_parent"
			android:layout_height="wrap_content"
			android:gravity="center_vertical"
			android:textColor="@color/dark_grey"
			android:layout_marginTop="8dp"
			tools:text="A network problem has occurred"/>

	<!-- body -->
	<TextView
			android:id="@+id/dialog_body"
			style="@style/TextStyle.Body1"
			android:layout_width="match_parent"
			android:layout_height="wrap_content"
			android:textColor="@color/grey_dark_light"
			android:layout_marginTop="16dp"
			android:layout_marginBottom="8dp"
			tools:text="Once you are back in network coverage, please retry your connection."/>
</LinearLayout>